**Arc Testnet USDC - EIP-3009 Support Request** 🚨

Hey Circle team! Working on a multi-chain USDC payment protocol for ETHGlobal Bangkok. Successfully integrated Base Sepolia, Polygon Amoy, Arbitrum Sepolia, and Optimism Sepolia, but hitting two blockers on Arc Testnet:

---

**Issue #1: EIP-3009 Signature Validation Fails** ❌

`transferWithAuthorization` reverts with `FiatTokenV2: invalid signature`

**What works:**
✅ Function exists in contract (`0x3600000000000000000000000000000000000000`)
✅ Implementation at `0x3910B7cbb3341f1F4bF4cEB66e4A2C8f204FE2b8` has selector `0xe3ee160e`
✅ Standard transfers work fine
✅ Contract returns: name="USDC", version="2", DOMAIN_SEPARATOR=`0x3611...c6b0`

**What's broken:**
❌ Calculated domain separator doesn't match on-chain value
```
Our calculation (standard EIP-712): 0x94e7...4b37
Actual from contract: 0x3611...c6b0
```
❌ Tested 10,000+ combinations (chainId, name, version, salt) - none match
❌ ERC-5267 `eip712Domain()` not implemented

**Theory:** Arc's dual-interface USDC (native + ERC-20 precompile) uses non-standard domain params?

**Comparison - same code works everywhere else:**
• Base Sepolia ✅
• Polygon Amoy ✅  
• Arbitrum Sepolia ✅
• Optimism Sepolia ✅
• Arc Testnet ❌ (only this fails)

---

**Issue #2: Contract Deployment Timeouts** ⏱️

Can't deploy contracts to Arc Testnet - both QuickNode and public RPC timeout after 30s.
Read ops work, write ops fail.

---

**Questions:**

1. What are the correct EIP-712 domain parameters for Arc USDC?
2. Is EIP-3009 fully supported on Arc's native USDC precompile?
3. How can we query the actual domain (eip712Domain() fails)?
4. Is Arc Testnet RPC currently operational for deployments?

---

**Environment:**
• Chain: Arc Testnet (1243)
• USDC: `0x3600000000000000000000000000000000000000`
• RPC: https://rpc-testnet.arc.network (also tried QuickNode)
• Explorer: https://explorer.arc-testnet.circlechain.xyz
• Tools: ethers.js v6, Foundry

**Test balances:** Buyer: 10.02 USDC, Seller: 0.01 USDC

---

**Reproducible test:**
```typescript
const domain = {
  name: await usdc.name(),        // "USDC"
  version: await usdc.version(),  // "2"
  chainId: 1243,
  verifyingContract: "0x3600000000000000000000000000000000000000"
};

const signature = await signer.signTypedData(domain, types, transferAuth);
const { v, r, s } = ethers.Signature.from(signature);

const tx = await usdc.transferWithAuthorization(
  from, to, value, validAfter, validBefore, nonce, v, r, s
);
// ❌ Reverts: "FiatTokenV2: invalid signature"
```

---

**Workaround:** Can use standard `transferFrom` for Arc only, but loses gasless benefit.

**Need help by:** Nov 24 (hackathon deadline)

Detailed analysis: `ARC_EIP3009_ISSUE.md`, `ARC_USDC_ANALYSIS.md` (can share full repo if needed)

Thanks! 🙏

